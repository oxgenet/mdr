<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Documents may link images by URL, which the WebView fetches under the
         policy the Rust core enforces. Both permissions are also merged in from
         the Play Billing library's manifest, but they are declared here so the
         app states its own requirements: removing billing later would otherwise
         silently break remote images, with nothing to connect cause to effect. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- The launcher icon is derived from the same 1024px artwork the iOS
         bundle uses, so both stores show one mark. It is a legacy square icon:
         a proper adaptive icon needs separate foreground/background layers with
         a 66% safe zone, which is a design job rather than a resize. -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mdr">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Open a .md handed over by Files, a mail attachment, or any
                 other app — the Android counterpart of the iOS document type
                 registration (LSHandlerRank = Owner). Android matches on MIME
                 type, and Markdown arrives under several, so all are listed. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="text/x-markdown" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- "Share to mdr" from another app's share sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="text/x-markdown" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity" />
    </application>
</manifest>
